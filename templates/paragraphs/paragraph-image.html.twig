{%
include directory ~ '/templates/components/large.html.twig' with {
    'image' : paragraph.getImage().entity,
    'alt' : paragraph.getImage().value().title,
    'title' : paragraph.getImage().value().title
}
%}