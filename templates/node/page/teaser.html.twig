<a href="{{ page.getUrl() }}">
    <h3>{{ page.label() }}</h3>
    {%
        include directory ~ '/templates/components/thumbnail.html.twig' with {
            'image' : page.getImage().entity,
            'alt' : page.label(),
            'title' : page.label()
        }
    %}
</a>
